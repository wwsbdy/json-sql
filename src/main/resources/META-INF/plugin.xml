
<idea-plugin>
    <!--插件的id，注意不要跟其他插件重复，这个id全局唯一，尽可能复杂些-->
    <id>com.zj.jsonsql.JsonSql</id>
    <!--插件的名称-->
    <name>JsonSql</name>
    <vendor>zhoujie</vendor>

    <!--插件的描述信息，支持html，展示的位置参考图14-->
    <description><![CDATA[
    Json Sql<br>
    将json通过sql查出想要的数据<br>
    并把sql再转成json<br>
    插件位于Tools里<br>
    支持的sql语句有：
    <ul>
        <li>select *、name、name as alias、name.surname（多层查询）</li>
        <li>where =、!=、in、not in、>、>=、<、<=、between、like、not like、is null、is not null</li>
        <li>and、or、嵌套and和or</li>
        <li>order by asc、desc</li>
        <li>limit 1,2</li>
    </ul>
    不支持的sql语句有：
    <ul>
        <li>update、insert、delete</li>
        <li>group by</li>
        <li>函数</li>
        <li>连表</li>
        <li>distinct、union</li>
    </ul>
    一些例子：
    <ul>
        <li>select level.type.name t from arr where level.type.name in ('hello') order by t</li>
        <li>select name from a where name = 'hello' and (age >= 11 or level like '%lin%')</li>
    </ul>
    ]]></description>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Add your extensions here -->
        <!-- 自定义 底部view -->
        <executor id="JsonSql" order="last"
                  implementation="com.zj.jsonsql.ui.table.TableExecutor"/>
    </extensions>

    <!--跟build.gradle里的sinceBuild一致即可，意义相同，必须配置-->
    <idea-version since-build="201" until-build="233.*"/>

    <actions>
        <action id="com.kungyu.toolview.ConsoleViewAction" class="com.zj.jsonsql.ui.dialog.json.FromAction"
                text="JsonSql" description="JsonArray解析" icon="AllIcons.FileTypes.JsonSchema">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
